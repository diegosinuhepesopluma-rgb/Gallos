name: GalloGiro 1.21.1

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Descomprimir proyecto
        shell: bash
        run: |
          set -e

          ZIP=$(find . -maxdepth 1 -type f -name "*.zip" | head -n 1)

          if [ -z "$ZIP" ]; then
            echo "ERROR: No se encontró el ZIP"
            exit 1
          fi

          rm -rf proyecto_gallogiro
          mkdir proyecto_gallogiro

          unzip -o "$ZIP" -d proyecto_gallogiro

          BUILD_FILE=$(find proyecto_gallogiro -type f -name "build.gradle" | head -n 1)

          if [ -z "$BUILD_FILE" ]; then
            BUILD_FILE=$(find proyecto_gallog
